// If-else example

int main() {
  bool x = true;

  // Basic
  if (x) {
     print("X is true");
  } else {
     print("Not happening");
  }

   // Without else
   if (x) {
      print("Does things without else");
   }

   return 0;
}